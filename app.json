{
  "name": "saucerbot",
  "description": "Bot for the saucer groupme",
  "repository": "https://github.com/clarkperkins/saucerbot.git",
  "stack": "container",
  "env": {
    "DJANGO_SECRET_KEY": {
      "generator": "secret"
    },
    "HEROKU_APP_NAME": {
      "required": true
    },
    "GROUPME_CLIENT_ID": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "addons": [
    "bonsai",
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}